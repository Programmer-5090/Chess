{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Chess Game (lldb)", // Renamed for clarity
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/output/chess_game", // Corrected path
      "args": [],
      "cwd": "${workspaceFolder}", // Use workspaceFolder
      "preLaunchTask": "g++ Build Project" // Optional: runs your build task before debugging
    }
  ]
}